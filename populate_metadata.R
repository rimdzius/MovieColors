
populate_metadata <- function(database) {
      # Populate the metadata for the movie database:
      # title, releaseYear, director, genre, subgenre1, subgenre2, 
      # runtime, MPAARating, numberOfAwards, RTAudienceRating, budget, 
      # worldwideGrossEarnings
      
      
      
      # Combine title & release year
      # Search imdb.com for combined movie name
      # Select the first result
      # Gather the metadata
      # Search rottentomatoes for the combined movie name
      # select the first result
      # Gather the rating
      # Repeat for all movies
      
      
      return(database)
      
}